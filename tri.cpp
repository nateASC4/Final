<<<<<<< HEAD
#include <iostream>
#include <fstream>
#include <string>
#include "compact.h"
#include "node.h"

using namespace std;

  string Tri::Tri addWord(){
    Tri* head = new Tri();
    addNode("a");
    getNode();
    delete head;
  }
  void Tri::Tri rvmWord(){
    isTerminal = true;
    while (!isTerminal){
      getNode();
    }
    isTerminal = true;
    return
  }

  string Tri::Tri viewWords(string word){
    Tri* head = new Tri();
    //take a whole string
    checkFor(/** "specific string" **/)
    if head.checkFor() == false){
      printf ("Word not found")
      //Print message word does not exist.
    }
    else{
      return string;
    }
    return string;
  }

  int main(){

    return 0;
  }
